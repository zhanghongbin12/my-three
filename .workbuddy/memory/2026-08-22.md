# 2026-08-22 工作日志

## 3D 作品集实验室中文化

- 本地化 `src/App.vue` 页面文案：标题改为「3D 作品集实验室」，编号改为「作品 01 / 作品 02」，场景名改为「地球 / 汽车 / 行星场景 / 汽车模型展示」。
- 保留所有 Three.js 技术名词（Three.js、GLTFLoader、OrbitControls、ShaderMaterial、Mesh Material、Bloom Pass、Vue Component）。
- 更新 `public/index.html` 的 `<title>` 为中文、设置 `lang="zh-CN"`、翻译 `<noscript>` 提示。
- 根据用户选择，将 dock 按钮副标题从 `my-three1 / my-three2` 优化为 `场景 01 / 场景 02`。

## 汽车场景 Bloom 眩光优化

- 在 `src/components/CarView.vue` 的 GLTF 遍历中统一关闭模型自带自发光材质（`emissive` 设为黑色、`emissiveIntensity` 设为 0）。
- 调弱 `UnrealBloomPass` 参数：`threshold` 1.0 → 1.2、`strength` 1.5 → 0.45、`radius` 1 → 0.35，避免旋转时出现大面积白光眩光。

## 汽车场景环境贴图提亮

- 在 `src/components/CarView.vue` 中引入 `RoomEnvironment` 并通过 `PMREMGenerator` 生成环境贴图赋给 `scene.environment`。
- 设置 `ACESFilmicToneMapping` + `SRGBColorSpace` 输出，让金属车身反射更自然。
- 适度补光：主方向光 1.0 → 1.2，环境光 0.9 → 1.3，避免车身阴影死黑。

## GitHub Pages 自动部署

- 新增 `.github/workflows/deploy.yml`：push 到 main 时用 actions（checkout/setup-node/configure-pages/upload-pages-artifact/deploy-pages）自动构建并部署到 GitHub Pages。
- `vue.config.js` 增加 `publicPath: '/my-three/'`（仅生产环境），匹配项目站点子路径。
- `src/components/EarthView.vue` 纹理路径改为基于 `process.env.BASE_URL` 拼接，修复子路径下资源 404。
- 新增 `public/.nojekyll`；本地验证 `npm run build` 通过，dist/index.html 资源已带 `/my-three/` 前缀。
- 部署前提：GitHub 仓库 Settings → Pages 的 Source 需选择 "GitHub Actions"。



